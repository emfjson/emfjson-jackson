@namespace(uri="http://example.org/model", prefix="model")
package model;

@JSON(root="true")
class Parent {
   attr String ~id;
   val Child[*] children;
}

class Child {
   attr String ~id;
}
